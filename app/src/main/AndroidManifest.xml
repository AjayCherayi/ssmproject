<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.project007">

    <application
        android:allowBackup="true"
        android:icon="@drawable/rq"
        android:label="@string/app_name"
        android:roundIcon="@drawable/rq"
        android:supportsRtl="true"
        android:theme="@style/Theme.Project007">
        <activity android:name=".LoginSignup.Verifcation"></activity>
        <activity android:name=".LoginSignup.HomePage" />
        <activity android:name=".LoginSignup.Login" />
        <activity android:name=".LoginSignup.DashBoard" />
        <activity android:name=".NavBar" />
        <activity android:name=".LoginSignup.Sign" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>